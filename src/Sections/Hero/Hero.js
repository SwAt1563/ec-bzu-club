import './Hero.css'

import { Container } from 'react-bootstrap';

const Hero = () => {
    return (
        <>
            <Container fluid className="hero-container">
            </Container>
        </>
    )
};

export default Hero;